# cryoinspect-metrics

Repository for calculating quality metrics for cryo-EM data.

## Setup

Create and activate a Python environment. You may want to install the following
packages to run the notebooks:

```bash
pip install jupyterlab numpy pandas scipy matplotlib tifffile scikit-image napari
```

Then start JupyterLab:

```bash
jupyter lab
```
